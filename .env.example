# Public contact and discovery (safe to expose in the browser)
NEXT_PUBLIC_DISCOVERY_FORM_URL=
NEXT_PUBLIC_WHATSAPP_PHONE=
NEXT_PUBLIC_WHATSAPP_PREFILL=
NEXT_PUBLIC_CONTACT_EMAIL=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only. Never prefix with NEXT_PUBLIC.
# The demo runs without these keys using deterministic mock responses.
OPENAI_API_KEY=
DATABASE_URL=
